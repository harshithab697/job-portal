<%= form_with(model: dashboard_job, local: true) do |form| %>
  <% if dashboard_job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dashboard_job.errors.count, "error") %> prohibited this dashboard_job from being saved:</h2>

      <ul>
        <% dashboard_job.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :salary %>
    <%= form.number_field :salary %>
  </div>

  <div class="field">
    <%= form.label :experience %>
    <%= form.number_field :experience %>
  </div>

  <div class="field">
    <%= form.label :job_location %>
    <%= form.text_field :job_location %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
